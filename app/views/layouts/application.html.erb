<!DOCTYPE html>
<html>
  <head>
    <title>AdoptDontShop</title>
    <%=csrf_meta_tags%>

    <%=stylesheet_link_tag    'application', media: 'all'%>
    <%=javascript_include_tag 'application'%>
  </head>

  <body>
    <h4><%=link_to "Home", "/"%></h4>
    <h4><%=link_to "Pet Index", "/pets"%></h4>
    <h4><%=link_to "Shelter Index", "/shelters"%></h4>

    <% flash.each do |type, message| %>
      <p><%= message %></p>
    <% end %>

    <%=yield%>
  </body>
</html>
